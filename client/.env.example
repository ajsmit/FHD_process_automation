# This file is a client-only projection of the authoritative root template:
#   /.env.example
# Update /.env.example first, then keep this file synchronized.

# API base for browser calls
# Local:
NEXT_PUBLIC_API_BASE=http://localhost:3001/api/v1

# Production example:
# NEXT_PUBLIC_API_BASE=https://your-api-domain.example/api/v1
